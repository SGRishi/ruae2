# Frontend build variables
# Any one of these can be used by scripts/build-frontend.mjs
API_BASE=https://api.rishisubjects.co.uk
VITE_API_BASE=
NEXT_PUBLIC_API_BASE=

# Worker secrets/vars (set via wrangler secret/vars, not in git)
SESSION_SECRET=replace-with-long-random-value
PASSWORD_PEPPER=replace-with-long-random-value
ADMIN_KEY=replace-with-long-random-admin-password
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
ALLOWED_ORIGINS=https://rishisubjects.co.uk
PAGES_PROJECT_NAME=rishisubjects
REQUIRE_MANUAL_APPROVAL=true
ALLOW_LOCALHOST_ORIGINS=true
